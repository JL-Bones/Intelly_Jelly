GOOGLE_API_KEY=your_google_api_key_here

# Optional: Ollama Configuration (used when AI_PROVIDER is set to "ollama" in config.json)
# OLLAMA_BASE_URL is configured in config.json (defaults to http://localhost:11434)